const urlParams = new URLSearchParams(window.location.search);
const id = urlParams.get('id'); 

switch(id){
    case 1:
        // First subtopic
    case 2:
        // Second subtopic
    case 3:
        // Third subtopic
}